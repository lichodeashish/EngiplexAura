import React from 'react';

// This component is no longer used as video features have been removed.
// It is kept as an empty file to prevent breaking the build if referenced elsewhere.

const ExportVideoModal: React.FC = () => {
  return null;
};

export default ExportVideoModal;