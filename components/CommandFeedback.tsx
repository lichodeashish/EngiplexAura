import React from 'react';

// This component is no longer used as voice command features have been removed.
// It is kept as an empty file to prevent breaking the build if referenced elsewhere.

const CommandFeedback: React.FC = () => {
  return null;
};

export default CommandFeedback;